# Project - API Documentation

**Generated by IA-Factory Developer Agent**  
**Date**: 2026-05-27 00:21:33 UTC

---

## Overview

This API has 9 endpoints.

## Endpoints

### `/health`

#### 🟢 GET

**Defined in**: `backend/auth-service/main.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/health
```

---

### `/login`

#### 🟡 POST

**Defined in**: `backend/auth-service/api/auth.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/login \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/me`

#### 🟢 GET

**Defined in**: `backend/auth-service/api/auth.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/me
```

---

### `/register`

#### 🟡 POST

**Defined in**: `backend/auth-service/api/auth.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/register \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/{news_id}`

#### 🔴 DELETE

**Defined in**: `backend/news-service/api/news.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X DELETE http://localhost:3000/{news_id}
```

#### 🟢 GET

**Defined in**: `backend/news-service/api/news.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/{news_id}
```

#### 🟣 PATCH

**Defined in**: `backend/news-service/api/news.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X PATCH http://localhost:3000/{news_id} \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/{news_id}/classify`

#### 🟡 POST

**Defined in**: `backend/news-service/api/news.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/{news_id}/classify \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/{news_id}/feedback`

#### 🟡 POST

**Defined in**: `backend/news-service/api/news.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/{news_id}/feedback \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---
